import { connect } from 'react-redux';
import PinnedCitiesList from './components/PinnedCitiesList';

const mapDispatchToProps = (dispatch) => {
  return {

  };
};


export default connect(null, mapDispatchToProps)(PinnedCitiesList);
